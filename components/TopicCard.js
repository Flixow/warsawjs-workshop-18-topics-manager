import {Card} from './'

const TopicCard = ({title, treiners}) => (
  <Card
    title={title}
    Footer={() => (
      <div>
        ♡ 23
      </div>
    )}
  >

  </Card>
);

export default TopicCard;
